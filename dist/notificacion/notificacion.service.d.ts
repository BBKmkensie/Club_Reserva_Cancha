import { Repository } from 'typeorm';
import { Notificacion } from '../entities/notificacion.entity';
import { Alumno } from '../entities/alumno.entity';
import { Profesor } from '../entities/profesor.entity';
import { Admin } from '../entities/admin.entity';
import { MailService } from '../mail/mail.service';
import { NotificacionStreamService } from './notificacion-stream.service';
export declare class NotificacionService {
    private repo;
    private alumnoRepo;
    private profesorRepo;
    private adminRepo;
    private mailService;
    private streamService;
    constructor(repo: Repository<Notificacion>, alumnoRepo: Repository<Alumno>, profesorRepo: Repository<Profesor>, adminRepo: Repository<Admin>, mailService: MailService, streamService: NotificacionStreamService);
    crear(alumnoId: number, titulo: string, mensaje: string, tipo?: string): Promise<Notificacion>;
    crearParaProfesor(profesorId: number, titulo: string, mensaje: string, tipo?: string): Promise<Notificacion>;
    crearParaAdmin(adminId: number, titulo: string, mensaje: string, tipo?: string, refId?: number, enviarCorreo?: boolean): Promise<Notificacion>;
    notificarCoordinadoresPropuestaApoderado(params: {
        propuestaId: number;
        apoderadoNombre: string;
        alumnoNombre: string;
        alumnoRut?: string | null;
        tallerNombre: string;
        horarioPropuesto?: string | null;
        mensajeApoderado?: string | null;
        actividadDescripcion?: string | null;
        esActividadLibre?: boolean;
    }): Promise<void>;
    notificarCoordinadoresAusencia(titulo: string, mensaje: string, tipo?: string): Promise<void>;
    notificarCoordinadores(titulo: string, mensaje: string, tipo?: string, refId?: number): Promise<void>;
    findByAlumno(alumnoId: number): Promise<Notificacion[]>;
    findByProfesor(profesorId: number): Promise<Notificacion[]>;
    findByAdmin(adminId: number): Promise<Notificacion[]>;
    contarNoLeidas(alumnoId: number): Promise<number>;
    contarNoLeidasProfesor(profesorId: number): Promise<number>;
    contarNoLeidasAdmin(adminId: number): Promise<number>;
    marcarLeida(id: number, alumnoId: number): Promise<Notificacion>;
    marcarTodasLeidas(alumnoId: number): Promise<void>;
    marcarLeidaAdmin(id: number, adminId: number): Promise<Notificacion>;
    marcarTodasLeidasAdmin(adminId: number): Promise<void>;
    marcarLeidaProfesor(id: number, profesorId: number): Promise<Notificacion>;
    marcarTodasLeidasProfesor(profesorId: number): Promise<void>;
    eliminarAlumno(id: number, alumnoId: number): Promise<void>;
    eliminarProfesor(id: number, profesorId: number): Promise<void>;
    eliminarAdmin(id: number, adminId: number): Promise<void>;
}
